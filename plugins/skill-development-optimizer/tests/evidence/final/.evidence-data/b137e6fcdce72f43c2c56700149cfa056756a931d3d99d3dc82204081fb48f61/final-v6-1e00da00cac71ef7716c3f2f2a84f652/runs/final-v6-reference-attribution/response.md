Selected plan: `content` profile for the attributed reference and code example; no `SKILL.md` change, behavioral freeze, or importer evidence is needed.

Key validation evidence:

- Clean `inspect_skill.py` and `classify_change.py` outputs against the exact task diff.
- Configured Content-profile validation only.
- Quick prerequisites plus content contracts, executable/example accuracy, attribution, source-link verification, originality, and provenance.
- Artifact identity via changed paths, revision/file hashes, config/schema, commands, duration, and exit status.
- Stop on missing checks, unverifiable attribution/example, unknown or mixed paths, artifact mismatch, or unapproved network access.

The current checkout-wide classification is contaminated by unrelated/untracked worktree content, so it must not be used as task evidence. No files were modified.

Relative skill files actually read:

- `SKILL.md`
- `references/validation-profiles.md`
- `assets/skill-optimizer.example.json`