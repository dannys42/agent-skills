Validation plan for the requested trigger-description + mandatory recommendation-workflow edit:

1. Classification/profile: treat both edits as behavioral (discovery/selection + required workflow). Minimum profile = Behavior. Escalate to Full only if inspection/classification finds another non-test category, unknown paths, or this is actually a new plugin/release. Do not downgrade.
2. Pre-edit RED gate (writing-skills requirement): preserve/hash the baseline artifact; declare fresh-agent cases and rubric before editing; run baseline cases without the proposed change and capture the exact failure/rationalization. Include positive trigger cases, near-miss/non-trigger cases, mandatory-recommendation compliance cases, and at least one pressure case combining 3+ pressures (e.g. urgency + authority + sunk cost). Because the configured fresh-agent evaluator is unavailable, this mandatory RED evidence cannot currently be produced. Therefore the edit must not proceed under the writing-skills iron law; report validation status as incomplete/blocked by missing evaluator, never passed.
3. Deterministic work that can still be prepared: run inspect_skill.py and classify_change.py against the checked-in config; declare stopping criteria before checks; prepare case/rubric JSON; run the selected Behavior checks through run_validation.py without network; check structure/frontmatter/links/focused tests; ensure agents/openai.yaml remains consistent if its UI trigger text is affected. Stop on any failure, unknown/mixed category, or missing mandatory check.
4. GREEN/REFACTOR once fresh evaluation is restored: make the minimal wording change that addresses baseline failures; complete deterministic checks; hash the exact distributable artifact with hash_artifact.py (sha256-length-framed-v1); initialize exactly one headline cohort for that hash; run every declared case with a fresh agent that receives only the frozen skill and case prompt. Score declared rubric. Any distributable-byte change makes the cohort historical and requires rerunning all headline cases.
5. Behavioral rubric should separately cover: intended requests trigger; near misses do not over-trigger; recommendation is emitted in every mandatory context; required sequencing/stop behavior is followed; no shortcut based only on frontmatter; no undeclared workflow substitutions; artifact/read-report provenance is complete.
6. Evidence per case: case prompt, initialized run ID, verbatim response, verbatim separate files-read report, files_read_kind=`agent-reported`, and every/only declared boolean rubric key. Since file access is only self-reported, explicitly state that it is evidence—not independent instrumentation—and does not prove OS access, tool invocation, or full context isolation. Never relabel it independently-observed.
7. Completion evidence/report: exact artifact digest + manifest, config/profile/classification output, deterministic command outputs and exit statuses, cohort/evidence identifiers, verify + summarize output, checks run/skipped, missing checks, duration/work-vs-waiting time, evidence status, and next escalation condition. Stop and do not claim completion while the evaluator is unavailable or any run is incomplete/artifact-mismatched.

Separate list — relative files under the target skill directory actually read:
- SKILL.md
- references/validation-profiles.md
- references/frozen-evaluations.md

I executed the six scripts only with `--help`; I did not read their source, so they are intentionally absent from the read-file list.