Validation plan derived from the requested skill:

Selected profile: FULL. Deterministic `inspect_skill.py` found the four plugin manifests plus SKILL.md, agents metadata, 3 references, scripts, and tests. `classify_change.py ... --json` returned `full` with behavior, content, metadata, production, tests, and unknown categories. This is mandatory anyway because it is a new-plugin release and a mixed change. Do not downgrade.

Stopping criteria (declare before checks): stop on any failed or missing mandatory check; unknown/unaccounted path; manifest/name/version/source drift; broken or unverifiable reference/example/attribution/originality claim; test failure; distributable-byte change after freezing; artifact/evidence/cohort mismatch; incomplete fresh-agent case; failed clean install/discovery; accidental cache/credential/local-data inclusion; or need for unapproved live network access. Never count unavailable checks as passes.

Plan:
1. Freeze the intended release diff. Remove generated `__pycache__`/`.pyc` and other non-distributables; inspect `git status`, `git diff --check`, diff/stat, and privacy/secrets/local-path exposure. Re-run `inspect_skill.py CONFIG --json` and `classify_change.py CONFIG --json`; every path must be classified or explicitly resolved. Current unknown paths include the plan doc and example optimizer config, so the current tree is not release-ready evidence.
2. Quick/package layer: run the skill quick validator, plugin validator, JSON/YAML parsing/schema checks, and `run_validation.py CONFIG full --output VALIDATION.json`. Check folder/manifest structure, no TODOs, strict semver, required interface fields, asset/component paths, and cross-manifest consistency across `.claude-plugin`, `.codex-plugin`, `.cursor-plugin`, `gemini-extension.json`, and all marketplace adapters. Verify marketplace entry name, source path, policy/category, ordering, and version/source mapping to the exact release.
3. Content layer: validate every changed reference and example against declared content contracts; verify links/source attribution, example accuracy/typechecking where applicable, and originality/provenance. Missing attribution or unverifiable claims block release.
4. Script layer: preserve RED→GREEN evidence for new/changed behavior; run focused unit tests per changed script, full plugin unit/integration tests, CLI `--help` and representative success/failure-path smoke tests. Store commands, tool versions, exit codes, and output in the validation report. If a changed script actually fetches externally or manages an importer/cache, add the Importer profile threat matrix (pacing/retries/redirects, hostile cache objects and ancestor replacement, atomic interrupted publication, cleanup ownership, closed handles, unsupported-platform failures). A live check is optional only at an explicit milestone and requires approval.
5. Behavioral layer: after deterministic checks pass, hash the exact distributable skill with `hash_artifact.py CONFIG --json --output ARTIFACT.json` (`sha256-length-framed-v1`). Declare cases and rubric before cohort init. Initialize one headline cohort with `manage_evidence.py init`, then run each case in a fresh agent/task given only the prompt and frozen skill. Capture prompt, verbatim response, assigned run ID, separately reported files read and its evidence kind, and every rubric boolean. Complete, verify, and summarize with `manage_evidence.py`; self-reported files read are evidence, not OS instrumentation. Any distributable byte change makes the cohort historical and requires rerunning every headline case; never combine versions.
6. Release identity/install evidence: bind the behavioral artifact hash, validation report, evidence summary, manifest set, plugin archive/package hash if produced, commit SHA/tag, and toolchain versions. Install/reinstall from the intended local marketplace/revision, verify the selected marketplace points to that exact source, then use a fresh Codex task to prove discovery plus representative trigger/workflow behavior. Failed installation/discovery blocks release.
7. Timing/evidence closure: retain work-vs-wait timing reports and summarize them with `report_timing.py --json`; record checks run and skipped, reasons, duration, evidence status, and next escalation condition. The only legitimate conditional skip is Importer/live-network work when the scripts contain no acquisition/cache behavior; document that rationale rather than calling it passed.

Relative files actually read (relative to the specified skill directory):
- `SKILL.md`
- `references/validation-profiles.md`
- `references/frozen-evaluations.md`
- `references/importer-threat-model.md`

I invoked the six scripts with `--help` and ran `inspect_skill.py`/`classify_change.py`, but did not read their source files, so I have not listed them as files read.